# terraform-provider-domeneshop

Read-only mirror. Do not commit here.

This repository is generated automatically from
[sebastka/domeneshop-sdk](https://github.com/sebastka/domeneshop-sdk) — the package lives at
`packages/terraform-provider-domeneshop`, and every push to the monorepo overwrites this one. Open issues and
pull requests against the monorepo instead.

This placeholder is replaced by the package README on the first split.
